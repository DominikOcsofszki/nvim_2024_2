return {
	'akinsho/bufferline.nvim', version = "*", dependencies = 'nvim-tree/nvim-web-devicons',
	opts ={
		-- options={ mode = "buffers", -- set to "tabs" to only show tabpages instead}
		options={ mode = "tabs", -- set to "tabs" to only show tabpages instead}
}}
}
